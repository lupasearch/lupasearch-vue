import { BadgeElement, BadgeGenerateOptions } from '../search-results/BadgeOptions'

export type SearchBoxBadgeOptions = {
  elements: BadgeElement[]
  generate?: BadgeGenerateOptions
  anchorElementKey: string
}
